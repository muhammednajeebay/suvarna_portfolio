[build]
  publish = "."

# Required for Netlify CMS (admin panel) to serve admin/index.html
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200

# Optional: SPA catch-all (useful if you have routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
